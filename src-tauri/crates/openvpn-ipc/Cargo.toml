[package]
name = "openvpn-ipc"
version = "0.1.0"
description = "Shared IPC types: commands (connect, disconnect, status, logs) and JSON-serializable responses for the GUI–daemon Unix socket protocol."
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
